imports:
    - { resource: ../config.yml }
framework:
    annotations: true
    validation:
        enabled: true
        enable_annotations: true

overblog_graphql:
    errors_handler:
        debug: true
    definitions:
        class_namespace: "Overblog\\GraphQLBundle\\TypeShorthand\\__DEFINITIONS__"
        schema:
            query: RootQuery

overblog_graphql_configuration_yaml:
    mapping:
        directories:
            - "%kernel.project_dir%/config/typeShorthand/mapping"
