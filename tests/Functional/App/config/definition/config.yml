imports:
    - { resource: ../config.yml }

overblog_graphql:
    definitions:
        schema:
            query: RootQuery

overblog_graphql_configuration_yaml:
    mapping:
        directories:
            - "%kernel.project_dir%/config/definition/mapping"
